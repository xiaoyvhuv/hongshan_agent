# Frontend configuration. Copy to .env.local for local development.
# This URL must point to the running FastAPI backend.
VITE_API_BASE=http://127.0.0.1:8765

# Backend configuration is in backend/.env.example.
# Copy backend/.env.example to backend/.env and add your Bailian key there.
